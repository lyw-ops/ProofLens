\section{Example}

The formal theorem is implemented as \lean{Sample.add_zero_twice}.

The helper lemma is \lean{Sample.add_zero_once}.

\begin{lstlisting}[language=Lean]
theorem add_zero_twice (n : Nat) : (n + 0) + 0 = n := by
  rw [add_zero_once]
  exact add_zero_once n
\end{lstlisting}

